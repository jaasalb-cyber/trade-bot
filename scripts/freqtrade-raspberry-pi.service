[Unit]
Description=Freqtrade Docker Bot With Git Pull On Start
After=network-online.target docker.service
Wants=network-online.target
Requires=docker.service

[Service]
Type=oneshot
RemainAfterExit=yes
User=pi
Group=docker
WorkingDirectory=/home/pi/trade-bot
Environment=FT_BRANCH=stable
# Uncomment and adjust these to use a Bybit config on the Pi.
# Environment=FT_CONFIG=/freqtrade/user_data/config.bybit.json
# Environment=FT_STRATEGY=SampleStrategy
ExecStart=/home/pi/trade-bot/scripts/run-on-raspberry-pi.sh
ExecStop=/usr/bin/docker compose down
TimeoutStartSec=10min

[Install]
WantedBy=multi-user.target
